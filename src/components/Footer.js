import { Link } from "react-router-dom";
import "../CSS/prize.css";

const Footer = () => {
  return (
    <div className="foot">
      <Link to="/Prize">Prizes</Link>
    </div>
  );
};

export default Footer;
